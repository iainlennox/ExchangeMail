using Microsoft.AspNetCore.SignalR;

namespace ExchangeMail.Web.Hubs
{
    public class MailHub : Hub
    {
        // We can add methods here if clients need to send messages to the server,
        // but for now, we only need to push notifications to clients.
    }
}
